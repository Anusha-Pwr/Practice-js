/*
Functions in JavaScript are objects, and variables store references to them.
 Assigning one variable to another copies the reference, so both variables can call the same function even if one reference later changes.

Anonymous functions: give syntax error when used as function statements

function.length -> number of declared parameters before the first default parameter
                -> rest params are not counted
                -> length !== actual params passed

Default parameters: 
                -> default value is used only when argument is: undefined,
                                                                NOT for other falsy values (null, "")
                -> default parameters are evaluated at call time

                -> parameters have their own scope
*/





